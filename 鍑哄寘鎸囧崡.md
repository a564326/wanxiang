# 万象词条录 · 出包指南（Windows / 豆包环境）

环境已就绪：Java 17 + SDK cmdline-tools + platform-tools + build-tools 34 + platform 34 + NDK r26b + gradle 8.5，全部在 `G:\Android`。

## 第一步：把项目放到纯英文无空格路径

> ⚠️ **重要**：NDK 编译对中文路径极不友好。请把整个 `万象词条录_出包版_v2` 目录复制到如 `G:\dev\wanxiang`，再操作。

```cmd
copy /Y "原路径\万象词条录_出包版_v2" "G:\dev\wanxiang"
cd G:\dev\wanxiang
```

## 第二步：确认环境变量生效（新开 CMD）

```cmd
echo %ANDROID_HOME%        :: 应输出 G:\Android
echo %ANDROID_NDK_HOME%    :: 应输出 G:\Android\ndk\26.1.10909125
gradle -v                  :: 应显示 8.5
adb version                :: 应显示 Android Debug Bridge
```

若 `ANDROID_HOME` 为空，先设置（**新开窗口才生效**）：
```cmd
setx ANDROID_HOME "G:\Android"
setx ANDROID_NDK_HOME "G:\Android\ndk\26.1.10909125"
setx PATH "%PATH%;G:\Android\cmdline-tools\latest\bin;G:\Android\platform-tools;G:\Android\build-tools\34.0.0;G:\Android\gradle\bin"
```

## 第三步：一键回归（不依赖 SDK，先确认代码全绿）

```cmd
run_all.bat
```
看到 `全部通过 ✓` 再继续。

## 第四步：出包

```cmd
package.bat
```
- 首次构建会下载 Python-for-Android 依赖（约几百 MB，需联网），之后增量编译很快
- 成功后 APK 位于 `bin\万象词条录-0.2-debug.apk`
- 手机用「ES 文件管理器」之类拷入安装即可（需允许「安装未知来源应用」）

## 常见问题速查

| 报错 / 现象 | 原因 | 解决 |
|---|---|---|
| `Unable to find build-tools 33.0.2` | spec 版本与已装不符 | 已对齐 34.0.0，若仍报错检查 `buildozer.spec` |
| `NDK version not found` | NDK 路径未识别 | 确认 `ANDROID_NDK_HOME` 指向真实目录，且 spec 写 `android.ndk = 26b` |
| `encoding / 中文路径` 类错误 | 项目在中文目录 | **移到纯英文路径**（如 `G:\dev\wanxiang`） |
| `javac not recognized` | JDK 未装或 PATH 缺 | 装 JDK 17 并把 `bin` 加入 PATH |
| `Accept SDK license` 卡住 | 许可证未接受 | spec 已设 `accept_sdk_license = True`，仍不行则 `sdkmanager --licenses` |
| gradle 下载超时 | 官方源慢 | 用腾讯云镜像：`G:\Android\gradle\init.d\init.gradle` 写阿里云/腾讯云 repo |
| `AAPT2` 相关错误 | build-tools 损坏 | 重装 `sdkmanager "build-tools;34.0.0"` |

## 美术资源替换说明

`assets/` 下当前是**代码生成的暗金占位图**。若你有 AI 出图原图（主界面/抽卡/化道炉/经络/天外），**直接覆盖同名文件**即可，无需改任何代码：

- `icon.png` (1024×1024)、`bg.png` (1080×1920)
- `gacha_bg.png`、`smelt_bg.png`、`realm_bg.png`、`celestial_bg.png` (均 9:16 竖屏)

## 云端构建（备选）

若本地实在跑不动，把代码推到 GitHub，`.github/workflows/build.yml` 会自动出 APK（Actions → Artifacts 下载）。
